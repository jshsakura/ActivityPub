name: CICD (DISABLED)

# Disabled - using docker-build.yml for ARM64 builds instead
on:
  workflow_dispatch: # Manual trigger only

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
    - name: Workflow disabled
      run: echo "This workflow has been disabled. Using docker-build.yml for ARM64 builds."